import { makeStyles } from "@material-ui/core/styles";

export default makeStyles({
  search: {
    margin: "0 auto",
    width: "400",
    display: "flex",
  },
  form: {
    marginTop: "4rem",
  },
});
